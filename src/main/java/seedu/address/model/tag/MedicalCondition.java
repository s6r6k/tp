package seedu.address.model.tag;

/**
 * Represents a medical condition tag of a patient.
 */
public class MedicalCondition extends Tag {
    public MedicalCondition(String tagName) {
        super(tagName);
    }
}

